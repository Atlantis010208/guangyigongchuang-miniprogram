## 变更概览
- 在 `pages/profile/home/home.wxml` 的功能入口列表中新增一项“购物车”，点击跳转到底部 Tab `订单管理`（`/pages/cart/cart`）。
- 复用现有跳转逻辑：`go` 方法已对 `cart` 特殊处理为 `wx.switchTab`（`pages/profile/home/home.js:67-69`）。
- 更新 `docs/cloud-data-setup.md`：在“页面改造记录”和“使用与验证”中补充该入口与验证步骤。

## 实施步骤
- 在 `pages/profile/home/home.wxml` 的入口列表中，紧邻“订单”入口之后插入：
  ```xml
  <view class="cell" bindtap="go" data-url="/pages/cart/cart">
    <text>购物车</text>
    <text class="arrow">〉</text>
  </view>
  ```
  参考位置：`pages/profile/home/home.wxml:15` 后。
- 样式沿用 `.profile-card.unified .cells.inline .cell`（`pages/profile/home/home.wxss:13`），无需新增样式与资源。
- 保持 `home.js` 不变，现有 `go` 方法已涵盖 Tab 路由（`pages/profile/home/home.js:67-69`）。

## 文档更新
- 在 `docs/cloud-data-setup.md` → “页面改造记录”末尾新增一条：
  - 个人中心新增购物车入口：文件 `pages/profile/home/home.wxml`，事件 `go` → `wx.switchTab('/pages/cart/cart')`。
  - 验证：在个人中心点击“购物车”入口，成功切换到底部 Tab 的“订单管理”页面。
- 在 “使用与验证” 章节新增“小程序个人中心入口验证”小节，描述用户操作与期望行为：点击后切换、列表正常显示。

## 验证
- 启动小程序，进入“个人中心”页（`pages/profile/home/home`）。
- 点击“购物车”入口，确认跳转到 `订单管理`（`pages/cart/cart` Tab）。
- 检查 `pages/cart/cart.wxml` 的列表与筛选（商城订单/方案订单）是否正常展示。

## 兼容性与注意
- `cart` 为 Tab 页，必须使用 `wx.switchTab`；已由 `go` 方法自动处理，无需重复判断。
- 不影响既有“订单”入口；如需避免重复，可后续按需将“订单”更名为“订单管理”。

## 代码定位参考
- 入口插入点：`pages/profile/home/home.wxml:15`
- 路由逻辑：`pages/profile/home/home.js:67-69`
- 文档位置：`docs/cloud-data-setup.md`（“页面改造记录”、“使用与验证”）