## 目标
- 在现有 `docs/cloud-data-setup.md` 中新增“已完成任务清单”与“验证状态”章节，集中记录已交付的工作项与对应代码位置，保持单一文档汇总。

## 文档更新内容
- 已完成任务清单：
  - 新增并部署 `initCollections` 云函数（创建集合）
  - 启动时首启初始化集合并做本地标记（所有环境）
  - 统一数据访问封装：`utils/api.js`（Users/Orders/Requests 仓库与 LRU）
  - 发布页改造：请求与订单同步写入云端，失败自动初始化后重试
  - 购物车页接入 `watch`，实时合并云端与本地列表
  - 故障排查与重试机制补充
  - 文档编写与入口说明
- 验证状态：记录已确认“发布后 requests 与 orders 均有记录”的结论与验证步骤。

## 交付文件
- 更新 `docs/cloud-data-setup.md`，不新增其他文档。

## 结果预期
- 单一MD文件即可完整检查已完成任务与定位代码；后续增量改造继续在此文档累计记录。