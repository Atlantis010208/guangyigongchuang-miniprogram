## 修改点
- 去除更多按钮的圆形边框（不影响点击区域与交互）
- 将更多按钮整体缩小约10px，保持图标清晰

## 实现
- 更新 `pages/request/progress/progress.wxss`：
  - `.btn.more` 设为 `border: none; background: transparent;` 并将宽高从 `120rpx/88rpx` 调整为 `110rpx/78rpx`
  - `.more-icon .dot` 调整为 `12rpx` 保持清晰且比例协调

## 验证
- 进度页底部“更多”按钮无边框且尺寸更紧凑，三点图标仍清晰可见